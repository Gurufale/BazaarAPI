server:
  port: 8082
spring:
  profiles:
    active: dev

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none

  application:
    name: USER-SERVICE

  h2:
    console:
      enabled: true

logging:
  level:
    root: INFO


